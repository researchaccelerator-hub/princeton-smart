package com.screenlake.data.enums

import androidx.annotation.Keep

/**
 * Enum class representing the mobile status.
 */
@Keep
enum class MobIleStatusEnum {
    /**
     * Indicates that the mobile status is terminated.
     */
    TERMINATED
}